# Project Documentation Context (Non-Obvious Only)

This workspace is currently empty. Add documentation context here as the project grows.

## IBM Bob ecosystem context

- Sibling directories (e.g. `galaxium-travels`, `odysseus`) are separate demo projects — do not assume shared code
- MCP server demos live under `bob-mcp-servers-main/servers/`; each is independently runnable
- `bob-demo/` contains IBM Bob field demo templates, not application source code
- API docs may lag behind code — treat source as authoritative over README
